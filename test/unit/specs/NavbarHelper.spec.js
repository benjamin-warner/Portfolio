import Vue from 'vue'
import Router from '../../src/router'
import * from '../../src/helpers/NavbarHelper'

describe('NavbarHelper.js', () => {
  it('should load router elements into navbar', () => {
  		???
  })
})
